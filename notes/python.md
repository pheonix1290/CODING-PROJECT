---------------------------------------------------------------------------
# What is Python
- python is coding anguage used for backend



## Statements
- (print) it is a statemnet used in python to output message or information to the user
- (input) it is a string used to hold data type that is a text
- (int) represnet integer ehich is a string used to hold numeric data types that our integers
- (flow) is a string used to hold data that our  decimal numbers.
   e.g 
   
## Variables
- in python we dont use div to assign class / id to give value a name we call it variable.
      
                          variable_name = value 